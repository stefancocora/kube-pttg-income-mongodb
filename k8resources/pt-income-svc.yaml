apiVersion: v1
kind: Service
metadata:
  labels:
    name: pttg-test-mongodb
  name: pttg-test-mongodb
spec:
  type: NodePort
  ports:
  - name: https
    port: 443
    containerPort: https
    nodePort: ${ptincome_port}
  selector:
    name: pttg-test-mongodb